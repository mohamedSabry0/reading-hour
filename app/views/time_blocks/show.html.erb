<p>
  <strong>Name:</strong>
  <%= @time_block.name %>
</p>

<p>
  <strong>Amount:</strong>
  <%= @time_block.amount %>
</p>

<% @time_block.groups.each do |group|%>
  <p>
    <strong><%= group.name %></strong>
  </p>
<%end%>

<%= link_to 'Edit', edit_time_block_path(@time_block) %> |
<%= link_to 'Back', time_blocks_path %>
